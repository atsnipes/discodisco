/**
 * API Endpoints
 *
 * Add endpoint alias as the key and the url as property
 *
 * Add /:id (or whatever property name) to the url segments if you need
 * to pass data for puts, patch, get.
 *
 */
export default {
  token: '/oauth/request_token'
}
